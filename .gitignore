node_modules
dist
.nx/cache
.nx/workspace-data
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
nx-cloud.env
*.log
*.tsbuildinfo
apps/docs/src/lib/liveCode/dynamicImports.ts
.idea/material_theme_project_new.xml
coverage
.vitest-attachments
packages/components/out
.nx/workspace-data
coverage/*.json
.vitest
# macOS system files
.DS_Store
**/.DS_Store
packages/remote-react-components/.vitest-attachments

# The full react-docgen dump: a build input for the component index, the
# status registry and the remote-components generator. Outside `dist` so
# it stays out of the published tarball.
packages/components/.cache

# Local Claude Code preview launch config
.claude/launch.json

# Scratch state for agent-driven plan execution (superpowers): task briefs,
# per-task reports, review diffs and the progress ledger. Session-local
# bookkeeping, never part of a change.
.superpowers
