import { createRemoteComponent } from "@remote-dom/react";
import { RemoteSimpleFormElement } from "@mittwald/flow-remote-elements";

export const SimpleForm = createRemoteComponent(
  "flr-simple-form",
  RemoteSimpleFormElement,
);
