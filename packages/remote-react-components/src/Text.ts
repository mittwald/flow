import { createRemoteComponent } from "@remote-dom/react";
import { RemoteTextElement } from "@mittwald/flow-remote-elements";

export const Text = createRemoteComponent("flr-text", RemoteTextElement, {});
