import { createRemoteComponent } from "@remote-dom/react";
import { RemoteModalElement } from "@mittwald/flow-remote-elements";

export const Modal = createRemoteComponent("flr-modal", RemoteModalElement, {});
