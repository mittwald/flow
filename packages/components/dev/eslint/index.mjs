import flowComponentName from "./flowComponentName.mjs";

/** Flow's own ESLint rules, registered under the `flow/` prefix. */
export default {
  rules: {
    "flow-component-name": flowComponentName,
  },
};
