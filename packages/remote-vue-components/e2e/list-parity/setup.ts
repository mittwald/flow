/*
 * The host's stylesheet, because two of the List's controls are chosen by media
 * query: the all-filters button carries `hide-on-desktop` unless the list has a
 * secondary filter, and the scenarios click whichever one the viewport shows.
 */
import "@mittwald/flow-react-components/all.css";
