layout-card:
  background-color:
    value: "{neutral.color.100}"
  corner-radius:
    value: "{corner-radius.l}"
  padding:
    value: "{size-px.l}"
  padding-mobile:
    value: "{size-px.m}"
  box-shadow:
    value: "{shadow.content}"
