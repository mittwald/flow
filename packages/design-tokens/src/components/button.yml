button:
  border-radius:
    value: "{size-px.xs}"
  padding-squished-x:
    value: "{size-px.m}"
  padding-squished-y:
    value: "{size-px.s}"
  padding:
    value: "{size-px.s}"
  focus-outline-color:
    value: "{primary.color.800}"
  focus-outline-offset:
    value: "{size-px.xxs}"

  # Small
  padding-small:
    value: "{size-px.xs}"
  padding-small-squished-x:
    value: "{size-px.s}"
  padding-small-squished-y:
    value: "{size-px.xs}"
  font-size-small:
    value: "{font-size.s}"

  # Accent Solid
  accent-solid-background-color:
    default:
      value: "{accent-solid-background-color.default}"
    hover:
      value: "{accent-solid-background-color.hover}"
    pressed:
      value: "{accent-solid-background-color.pressed}"
  accent-solid-foreground-color:
    default:
      value: "{accent-solid-foreground-color.default}"
    hover:
      value: "{accent-solid-foreground-color.hover}"
    pressed:
      value: "{accent-solid-foreground-color.pressed}"

  # Accent Plain
  accent-plain-background-color:
    default:
      value: "{accent-plain-background-color.default}"
    hover:
      value: "{accent-plain-background-color.hover}"
    pressed:
      value: "{accent-plain-background-color.pressed}"
  accent-plain-foreground-color:
    default:
      value: "{accent-plain-foreground-color.default}"
    hover:
      value: "{accent-plain-foreground-color.hover}"
    pressed:
      value: "{accent-plain-foreground-color.pressed}"

  # Primary Solid
  primary-solid-background-color:
    default:
      value: "{primary-solid-background-color.default}"
    hover:
      value: "{primary-solid-background-color.hover}"
    pressed:
      value: "{primary-solid-background-color.pressed}"
  primary-solid-foreground-color:
    default:
      value: "{primary-solid-foreground-color.default}"
    hover:
      value: "{primary-solid-foreground-color.hover}"
    pressed:
      value: "{primary-solid-foreground-color.pressed}"

  # Primary Plain
  primary-plain-background-color:
    default:
      value: "{primary-plain-background-color.default}"
    hover:
      value: "{primary-plain-background-color.hover}"
    pressed:
      value: "{primary-plain-background-color.pressed}"
  primary-plain-foreground-color:
    default:
      value: "{primary-plain-foreground-color.default}"
    hover:
      value: "{primary-plain-foreground-color.hover}"
    pressed:
      value: "{primary-plain-foreground-color.pressed}"

  # Secondary Solid
  secondary-solid-background-color:
    default:
      value: "{secondary-solid-background-color.default}"
    hover:
      value: "{secondary-solid-background-color.hover}"
    pressed:
      value: "{secondary-solid-background-color.pressed}"
  secondary-solid-foreground-color:
    default:
      value: "{secondary-solid-foreground-color.default}"
    hover:
      value: "{secondary-solid-foreground-color.hover}"
    pressed:
      value: "{secondary-solid-foreground-color.pressed}"

  # Secondary Plain
  secondary-plain-background-color:
    default:
      value: "{secondary-plain-background-color.default}"
    hover:
      value: "{secondary-plain-background-color.hover}"
    pressed:
      value: "{secondary-plain-background-color.pressed}"
  secondary-plain-foreground-color:
    default:
      value: "{secondary-plain-foreground-color.default}"
    hover:
      value: "{secondary-plain-foreground-color.hover}"
    pressed:
      value: "{secondary-plain-foreground-color.pressed}"

  # Danger Solid
  danger-solid-background-color:
    default:
      value: "{danger-solid-background-color.default}"
    hover:
      value: "{danger-solid-background-color.hover}"
    pressed:
      value: "{danger-solid-background-color.pressed}"
  danger-solid-foreground-color:
    default:
      value: "{danger-solid-foreground-color.default}"
    hover:
      value: "{danger-solid-foreground-color.hover}"
    pressed:
      value: "{danger-solid-foreground-color.pressed}"

  # Danger Plain
  danger-plain-background-color:
    default:
      value: "{danger-plain-background-color.default}"
    hover:
      value: "{danger-plain-background-color.hover}"
    pressed:
      value: "{danger-plain-background-color.pressed}"
  danger-plain-foreground-color:
    default:
      value: "{danger-plain-foreground-color.default}"
    hover:
      value: "{danger-plain-foreground-color.hover}"
    pressed:
      value: "{danger-plain-foreground-color.pressed}"

  # Disabled Solid
  disabled-solid-background-color:
    value: "{disabled-solid-background-color}"
  disabled-solid-foreground-color:
    value: "{disabled-solid-foreground-color}"

  # Disabled Plain
  disabled-plain-background-color:
    value: "{disabled-plain-background-color}"
  disabled-plain-foreground-color:
    value: "{disabled-plain-foreground-color}"
