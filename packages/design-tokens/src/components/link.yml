link:
  font-size:
    value: "{font-size.s}"
  line-height:
    value: "{line-height.m}"
  font-weight:
    value: "{font-weight.bold}"
  disabled-color:
    value: "{disabled-plain-content-color}"
  disabled-color-light:
    value: "{disabled-light-plain-content-color}"
  disabled-color-dark:
    value: "{disabled-dark-plain-content-color}"
  text-to-icon-spacing:
    value: "{size-rem.xs}"

  color:
    default:
      value: "{primary-plain-content-color.default}"
    hover:
      value: "{primary-plain-content-color.hover}"
    pressed:
      value: "{primary-plain-content-color.pressed}"
  color-dark:
    default:
      value: "{dark-plain-content-color.default}"
    hover:
      value: "{dark-plain-content-color.hover}"
    pressed:
      value: "{dark-plain-content-color.pressed}"
  color-light:
    default:
      value: "{light-plain-content-color.default}"
    hover:
      value: "{light-plain-content-color.hover}"
    pressed:
      value: "{light-plain-content-color.pressed}"
