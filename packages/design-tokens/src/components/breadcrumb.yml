breadcrumb-item:
  color:
    value: "{content.color}"
  padding-x:
    value: "{size-px.xs}"
  font-size:
    value: "{font-size.s}"
  font-weight:
    default:
      value: "{font-weight.normal}"
    current:
      value: "{font-weight.bold}"
