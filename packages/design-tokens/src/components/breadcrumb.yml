breadcrumb-item:
  color:
    value: "{heading.color}"
  color-static-white:
    value: "{static-white-plain-content-color.default}"
  color-static-black:
    value: "{static-black-plain-content-color.default}"
  padding-x:
    value: "{size-px.xs}"
  font-size:
    value: "{font-size.s}"
  font-weight:
    default:
      value: "{font-weight.normal}"
    current:
      value: "{font-weight.bold}"
