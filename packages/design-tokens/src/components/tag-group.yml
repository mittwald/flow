tag-group:
  tag-to-tag-spacing:
    value: "{size-px.s}"
  label-to-tag-spacing:
    value: "{size-rem.xs}"

tag:
  font-size:
    value: "{font-size.s}"
  font-weight:
      value: "{font-weight.bold}"
  border-radius:
    value: "{corner-radius.default}"
  padding-x:
    value: "{size-px.s}"
  padding-y:
    value: "{size-px.xxs}"
  background-color:
    default:
      value: "{neutral.color.400}"
    hover:
      value: "{info.color.100}"
    selected:
      value: "{info.color.200}"
    disabled:
      value: "{neutral.color.300}"
  color:
    selected:
      value: "{info.color.800}"
    disabled:
      value: "{neutral.color.600}"
