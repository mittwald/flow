label:
  font-size:
    value: "{font-size.s}"
  line-height:
    value: "{line-height.s}"
  font-weight:
    value: "{font-weight.bold}"
  color:
    default:
      value: "{neutral.color.800}"
    disabled:
      value: "{neutral.color.600}"
