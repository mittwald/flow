label:
  font-size:
    value: "{font-size.s}"
  font-weight:
    value: "{font-weight.bold}"
  line-height:
    value: "{line-height.m}"
  color:
    default:
      value: "{neutral.color.800}"
    disabled:
      value: "{neutral.color.600}"
