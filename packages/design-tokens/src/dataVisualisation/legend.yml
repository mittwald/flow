legend:
  item-spacing:
    value: "{size-px.m}"

legend-item:
  marker-to-label-spacing:
    value: "{size-rem.s}"
  marker:
    size:
      value: "{size-rem.m}"
    corner-radius:
      value: "{corner-radius.default}"
