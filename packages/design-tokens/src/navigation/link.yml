link:
  icon-height:
    value: "calc({font-size-text.m} * 1.25)"
  icon-height-s:
    value: "calc({font-size-text.s} * 1.25)"
  font-weight:
    value: "{font-weight.bold}"
  spacing:
    value: "{size-rem.xs}"

  color:
    default:
      value: "{primary.color.800}"
    hover:
      value: "{primary.color.900}"
    pressed:
      value: "{primary.color.1000}"
    disabled:
      value: "{disabled-plain-content-color}"

  # Dark
  color-dark:
    default:
      value: "{dark.color.800}"
    hover:
      value: "{dark.color.700}"
    pressed:
      value: "{dark.color.600}"
    disabled:
      value: "{disabled-plain-dark-content-color}"

  # Light
  color-light:
    default:
      value: "{light.color.800}"
    hover:
      value: "{light.color.700}"
    pressed:
      value: "{light.color.600}"
    disabled:
      value: "{disabled-plain-light-content-color}"

  # Static Dark
  color-static-dark:
    default:
      value: "{static-dark.color.800}"
    hover:
      value: "{static-dark.color.700}"
    pressed:
      value: "{static-dark.color.600}"
    disabled:
      value: "{disabled-plain-static-dark-content-color}"

  # Static Light
  color-static-light:
    default:
      value: "{static-light.color.800}"
    hover:
      value: "{static-light.color.700}"
    pressed:
      value: "{static-light.color.600}"
    disabled:
      value: "{disabled-plain-static-light-content-color}"
