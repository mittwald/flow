text:
  color:
    default:
      value: "{neutral.color.1100}"
    light:
      value: "{light-plain-content-color}"
    dark:
      value: "{dark-plain-content-color}"
    static-light:
      value: "{static-light-plain-content-color}"
    static-dark:
      value: "{static-dark-plain-content-color}"
    disabled:
      value: "{disabled-plain-content-color}"
  max-width:
    value: "780px"

  blockquote-border-width:
    value: "{border-width.300}"
  blockquote-border-style:
    value: "{border-style.default}"
  blockquote-border-color:
    value: "{info-outline-border-color}"
  blockquote-padding:
    value: "{size-rem.s}"

  list-padding:
    value: "{size-rem.l}"
