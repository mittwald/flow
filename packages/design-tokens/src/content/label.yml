label:
  font-size:
    value: "{font-size.s}"
  font-weight:
    value: "{font-weight.bold}"
  spacing:
    value: "{size-rem.xs}"
  color:
    default:
      value: "{neutral.color.800}"
    disabled:
      value: "{disabled-plain-content-color}"
