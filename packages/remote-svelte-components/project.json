{
  "implicitDependencies": ["components"],
  "name": "remote-svelte-components",
  "projectType": "library",
  "sourceRoot": "packages/remote-svelte-components/src",
  "targets": {
    "build": {
      "inputs": ["default", "{projectRoot}/src/**/*"],
      "dependsOn": [
        "^build",
        {
          "projects": ["components"],
          "target": "build:remote-components"
        }
      ]
    },
    "test:corpus": {
      "dependsOn": [
        "^build",
        {
          "projects": ["components"],
          "target": "build:remote-components"
        }
      ]
    }
  }
}
