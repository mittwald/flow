## With initials

Initials are automatically created from the text provided to the `<Initials />`
component.

<LiveCodeEditor />

## With image

Use an `<Image />` inside the `<Avatar />` to display a profile image.

<LiveCodeEditor example="image" />

## With icon

Any `<Icon />` can be displayed inside the `<Avatar />`.

<LiveCodeEditor example="icon" />

## Sizes

<LiveCodeEditor example="sizes" />

## Color variants

The color depends on the components children. This adds some visual variance
when displaying a list of avatars.

<LiveCodeEditor example="colors" />
