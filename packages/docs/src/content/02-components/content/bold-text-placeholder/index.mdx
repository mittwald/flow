## Example

Use the `BoldTextPlaceholder` if you need your text to have the width, it would
have with bold font weight, even if it is regular font weight (For example in
horizontal navigations, where the font weight of the active item changes to
bold).

<LiveCodeEditor />

<LiveCodeEditor example="comparison" />
