import { Markdown } from "@mittwald/flow-remote-react-components";

export default function Page() {
  return (
    <Markdown>
      The **Death Star** is fully operational and moving toward the *Endor*
      system, guarded by the ***entire Imperial fleet***.
    </Markdown>
  );
}
