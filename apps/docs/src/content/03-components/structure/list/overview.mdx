# Playground

Verwende `<List />`, um eine Liste darzustellen.

<LiveCodeEditor />

---

# Kombiniere mit ...

## Sortierung

Nutze `<List.Sorting />` innerhalb der List, um eine Sortiermethode anzulegen.

Benutzerdefinierte Sortierfunktionen ermöglichen spezielle Sortierlogik für
verschiedene Datentypen wie BigInt, inklusive Typprüfung und
Fallback-Mechanismen für ungültige Eingaben. Außerdem können eigene
Sortierfunktionen über die List.Sorting-Komponente mit dem Property
`customSortingFn` hinzugefügt werden.

<LiveCodeEditor example="sorting" editorCollapsed />

## Filter

Über `<List.Filter />` lassen sich Filtermöglichkeiten anlegen.

<LiveCodeEditor example="filter" editorCollapsed />

## Suche

Verwende `<List.Search />` innerhalb der List, um ein
[SearchField](/03-components/form-controls/search-field) anzuzeigen.
Standardmäßig wird die Suche bei Klick auf Enter gestartet. Um die Suche
automatisch zu starten, kann das Property `autoSubmit` gesetzt werden.

<LiveCodeEditor example="search" editorCollapsed />

## ActionGroup

Verwende `<ActionGroup/>` innerhalb der List, um eine
[ActionGroup](/03-components/actions/action-group) anzuzeigen. Hier können
Aktionen definiert werden, die sich direkt auf die Liste beziehen.

<LiveCodeEditor example="actionGroup" editorCollapsed />

## Summary

Verwende eine `<ListSummary/>` um eine Zusammenfassung, wie beispielsweise die
Summe der Beträge, anzuzeigen. Über das `position` Property kannst du bestimmen,
ob die Summary oberhalb oder unterhalb der Liste angezeigt werden soll.

<LiveCodeEditor example="summary" editorCollapsed />

## Mit Accordion

Aktiviere das Accordion-Verhalten über die `accordion` Property. So lässt sich
ein Listen-Element durch Klick ein- bzw. ausklappen. Der erweiterte Inhalt muss
dann in `<Content slot="bottom" />` enthalten sein.

<LiveCodeEditor example="accordion" editorCollapsed />

## Pagination

Die Pagination wird standardmäßig bei jeder List aktiviert. Sie zeigt im
Standard 10 Einträge an und lädt über einen Klick auf "Mehr anzeigen" jeweils 10
Einträge nach. Diese Werte lassen sich über die `batchSize` Property anpassen.

<LiveCodeEditor example="pagination" editorCollapsed />

---

# Ansichten

## Tabellen-Ansicht

Die List kann als Liste oder als Tabelle dargestellt werden. Nutze
`<List.Item />` für die Darstellung als Liste und `<List.Table />`, um sie als
[Tabelle](/03-components/structure/table) anzuzeigen

<LiveCodeEditor example="table" editorCollapsed />

## Raster-Ansicht

Im List.Item kann über die Properties `showTiles` und `showList` ausgewählt
werden wie die List Items dargestellt werden. `showList` ist im default auf true
gesetzt. Sind beide Werte gesetzt, kann über das Ansichts-Menü zwischen den
beiden Darstellungen gewechselt werden.

Über das optionale `maxTileWidth` Property kann die maximale Breite der Kacheln
angepasst werden.

<LiveCodeEditor example="tile" editorCollapsed />

---

# List Items

## Mit Checkboxen

Checkboxen, die in einem ListItem platziert werden, werden automatisch vorne
angeordnet. Die Funktionalität der Checkbox wird nicht von der Liste gesteuert
und muss individuell implementiert werden. Achte bei der Implementierung jedoch
darauf, dass die gesamte Zeile genutzt werden kann, um das Element zu markieren.
Nutze dafür `onAction` der List.

<LiveCodeEditor example="checkbox" editorCollapsed />

## Mit ColumnLayout

Dem ListItem können die ColumnLayout Properties `s`,`m` und `l` mitgegeben
werden, um das Seitenverhältnis und Umbruchverhalten von Header und Content zu
steuern.

<LiveCodeEditor example="columnLayout" editorCollapsed />

## Mit ActionGroup

Verwende eine ActionGroup innerhalb des `<Content/>` um Buttons in deiner Liste
zu platzieren

<LiveCodeEditor example="itemActionGroup" editorCollapsed />
