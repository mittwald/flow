# Playground

Verwende `<Table />`, um tabellarische Daten strukturiert darzustellen. Nutze `TableHeader` und `TableColumn`, um die Spaltenüberschriften zu definieren. Verwende `TableBody`, um den Content über `TableRow` und `TableCell` abzubilden.

<LiveCodeEditor />

---

# Mit Footer

Um die letzte Zeile der Table, z. B. für eine Preisübersicht, hervorzuheben
kann die `TableFooterRow` verwendet werden.

<LiveCodeEditor example="withFooter" editorCollapsed />

---

# Mit vertikal zentriertem Inhalt

Der Inhalt wird vertikal zentriert dargestellt, sodass dieser mittig zwischen dem oberen und unteren Zellenrand positioniert ist.

<LiveCodeEditor example="vertically-centered" editorCollapsed />

---

# Mit horizontal zentriertem Inhalt

Der Inhalt wird horizontal zentriert ausgerichtet und unterstützt optional die Statusanzeigen `success` und `danger`.

<LiveCodeEditor example="horizontally-centered" editorCollapsed />
