---
title: Boundaries
---

Error- und Loading-Boundaries sind ein zentrales Werkzeug, um Ladezustände
anzuzeigen und Fehler im UI gezielt abzufangen. Sie verhindern, dass Fehler
unkontrolliert die gesamte Anwendung zum Absturz bringen, und bieten die
Möglichkeit, Usern eine verständliche Rückmeldung zu geben. Gleichzeitig
unterstützen sie Entwicklern durch Logging und Monitoring bei der Fehleranalyse.

Da jede Anwendung unterschiedliche Anforderungen hat, gibt es kein universelles
Schema, wo und wie Boundaries am besten platziert werden. Die folgenden Best
Practices sollen bei der Orientierung und Entscheidungsfindung helfen.

---

<Alert>
  <Heading>Guideline für interne Teams</Heading>
  <Content>
    Diese Guideline richtet sich in erster Linie an das interne Team von
    mittwald und setzt entsprechendes Vorwissen voraus. Die darin genannten
    Components beziehen sich auf interne mStudio-Components.
  </Content>
</Alert>

# Grundprinzip: zwei Konzepte, zwei Scopes

Error- und Loading-Boundary sind **zwei getrennte Konzepte** mit
**unterschiedlicher Scope-Logik**. Sie werden über zwei Primitive ausgedrückt:

| Primitiv             | Enthält             | Zweck                                                                                                                     |
| -------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `…WithBoundaries`    | Loading **+** Error | Standardfall; meist weit oben an der Seiten-Wurzel, außerdem für enge Loading-Ausnahmen.                                  |
| `…WithErrorBoundary` | nur Error           | Enges Error-Scoping für sekundäre Daten. Der Loading-Zustand blubbert zur nächsten **weiten** Loading-Boundary nach oben. |

Eine reine Loading-Boundary gibt es bewusst **nicht** – sie wird nie gebraucht
(siehe [Loading-Scope](#loading-scope--layout-shift-vermeiden)).

# Wann eine Boundary nötig ist

Liest eine Component eine Datenquelle, **die Suspense auslöst**
(`react-use-promise` / Ghost-`.use()`, eine Suspense auslösende Query), muss sie
von Boundaries abgedeckt sein. Wie weit die Abdeckung reicht, klären die beiden
Scope-Regeln weiter unten.

**Umkehrung: kein Suspense → keine Boundary.** Löst der Render-Prop **kein**
Suspense aus (i18n-`useT…()`, Props und bereits oben geladene Cache-Hits zählen
**nicht**), braucht die Component **gar keine** Boundary – dann das **plain
Flow-Primitiv** verwenden (`<LayoutCard>`, `<Section>`), nicht
`…WithErrorBoundary`.

<Alert>
  <Heading>Cache-Hit löst kein Suspense aus</Heading>
  <Content>
    Ein `.use()` auf eine Ressource, die weiter oben auf derselben Seite bereits
    aufgelöst wurde, löst kein Suspense mehr aus. Die Boundary darum herum
    bewirkt nichts und gehört entfernt.
  </Content>
</Alert>

# Error-Scope = Kritikalität der Daten

- **Primär** – die Seite wäre ohne diese Daten sinnlos oder leer (Faustregel:
  die Ressource des Routen-Parameters, z. B. der Cronjob auf `$cronjobId`). →
  **Keine eigene enge Error-Boundary**; der Fehler fällt an die **weite Boundary
  an der Seiten-Wurzel**.
- **Sekundär** – die Seite bleibt ohne diese Daten nutzbar (z. B. eine
  verknüpfte App-Installation auf einer Cronjob-Seite). → **Eigene enge
  `…WithErrorBoundary`**, damit ein Fehler nur diesen Teil betrifft.

# Loading-Scope = Layout-Shift vermeiden

- **Default: weit.** Wenige, große Ladezustände statt vieler kleiner. Darum
  sitzt die weite kombinierte `…WithBoundaries` an der **Seiten-Wurzel**,
  oberhalb aller Sektionen.
- **Ausnahme: eng** für lang ladende, unabhängige Elemente (z. B. Metriken auf
  Dashboards). Hier ist ein enger Error-Scope ohnehin erwünscht, daher nutzt man
  eine enge kombinierte `…WithBoundaries`.

# Darstellung

Loading- und Error-Darstellung sind **symmetrisch**: Was im Ladezustand sichtbar
Platz reserviert, zeigt im Fehlerfall einen sichtbaren degradierten Zustand –
und umgekehrt darf unsichtbarer, bedingter Content in beiden Zuständen
verschwinden.

## Loading

- **Sichtbarer Content** → **form-treuer Skeleton, der den finalen Platz
  reserviert**. Weder ein Spinner noch ein impliziter `null` – beide reservieren
  den finalen Platz nicht und erzeugen den Layout-Shift, den die weite Boundary
  verhindern soll.
- **Bedingter / „unsichtbarer" Content**, der nach dem Laden `null` rendern kann
  (z. B. ein Alert, der erst Daten lädt, um über „sichtbar: ja/nein?" zu
  entscheiden) → `null` ist **korrekt**; ein Skeleton würde als Phantom
  aufblitzen. Das ist per Definition der Fall des bare `Fragment…`-Wrappers –
  für sichtbaren Content nie diesen nehmen, sondern einen typisierten Wrapper
  mit echtem Skeleton.

## Error

- **Sichtbarer** sekundärer Content → bei Fehler ein **sichtbarer** degradierter
  Zustand (kompakter Inline-Fehler), **nie stumm** verschwinden.
- Stummes `null`-on-error ist nur für den bare Fragment-Wrapper erlaubt. Sentry
  protokolliert den Fehler über die `ErrorBoundary` ohnehin – es geht rein um
  die UX.
- **Primär / weit** → volle `IllustratedMessage` an der Seiten-Wurzel.

# Reset / Retry

Reset bei Routenwechsel (`resetKeys`) plus `QueryErrorResetBoundary`; der
Error-Fallback bietet ein Retry an, wenn der Platz es zulässt. Die Aktion sollte
zum Scope passen – ein enges Sekundär-Widget darf nicht die ganze App neu laden.

# Wrapper-Wahl: Verhalten × Layout-Primitiv

Jeder Wrapper kombiniert ein **Boundary-Verhalten** (Loading-/Error-Scope) mit
einem **Layout-Primitiv**, das eigene visuelle Semantik trägt – die Wahl ist
also zweidimensional:

- `Section…` → rendert `<Section>` (vertikale **Abstände** zwischen den Kindern)
- `LayoutCard…` → rendert `<LayoutCard>` (**Card-Chrome**: Hintergrund, Padding,
  Rahmen)
- `Fragment…` / `SectionsFragment…` / `LayoutCardFragment…` → **kein**
  strukturelles Element (`<>`)

Beim Ändern des Scopes muss das Layout-Primitiv **bewusst erhalten** bleiben;
ein Scope-Wechsel darf Spacing/Chrome nie stillschweigend droppen:

- `SectionWithBoundaries` → `FragmentWithBoundaries` entfernt die `<Section>`
  und damit die Abstände → die Kinder explizit in eine `<Section>` wrappen.
- Ein `LayoutCard…` nicht gegen ein Fragment-Primitiv tauschen, wenn der Block
  eine Kachel bleiben soll (v. a. im Grid neben anderen `LayoutCard`s) – sonst
  verschwindet der Rahmen.

# Technische Umsetzung

Bei der Platzierung der Boundaries empfiehlt es sich, vom Groben ins Detail
vorzugehen. Entscheidend ist eine gute Balance: Zu viele kleine Boundaries
erhöhen die Komplexität, zu wenige gefährden hingegen die Stabilität großer
Teile der Anwendung.

## Inhalte einer LayoutCard

Sind Funktionen oder Informationen einer LayoutCard essenziell für die Nutzung
der Seite, sollte die gesamte LayoutCard mit `LayoutCardWithBoundaries`
umschlossen sein. Dabei wird die Anzahl der geladenen Sections als
`sectionsCount` mitgegeben, um die Loading View passend darzustellen.

### Listenseite

Eine Listenseite enthält in der Regel eine einzelne Section. Der `sectionsCount`
beträgt hier daher 1. Der Count der Liste wird zuerst geladen, damit die gesamte
Liste im Fehlerfall in die ErrorView wechseln kann.

```tsx
export const CronjobsPage: FC = () => (
  <LayoutCardWithBoundaries suspenseFallbackProps={{ sectionsCount: 1 }}>
    {() => {
      const { projectId } = usePathParams("projectId");
      const projectGhost = ProjectGhost.ofId(projectId);
      const cronjobCount = projectGhost.cronjobs.getTotalCount().use();

      return (
        <>
          <ProjectDeactivatedAlert project={projectGhost} />
          <SectionWithBoundaries>
            {cronjobCount === 0 ? (
              <CronjobIllustratedMessage project={projectGhost} />
            ) : (
              <CronjobList project={projectGhost} />
            )}
          </SectionWithBoundaries>
        </>
      );
    }}
  </LayoutCardWithBoundaries>
);
```

### Detailseite

Der `sectionsCount` sollte der erwarteten Anzahl an Sections entsprechen. So
bleibt das Layout während des Ladevorgangs stabil. Das Hauptelement der Seite –
die **Primär**-Ressource des Routen-Parameters – wird zuerst geladen, damit im
Fehlerfall für die gesamte Seite eine ErrorView angezeigt werden kann. Die
einzelnen `…Section`-Components lesen ausschließlich diese bereits aufgelöste
Primär-Ressource und bekommen daher **keine eigene Boundary** (siehe
[Einmalig genutzte Components](#einmalig-genutzte-components-keine-eigene-boundary-für-primärdaten)).

```tsx
export const CronjobPage: FC = () => (
  <LayoutCardWithBoundaries suspenseFallbackProps={{ sectionsCount: 2 }}>
    {() => {
      const { cronjobId } = usePathParams("cronjobId");
      const cronjob = CronjobGhost.ofId(cronjobId).getCommon().use();

      return (
        <>
          <ProjectDeactivatedAlert project={cronjob.project} />
          <GeneralSection cronjob={cronjob} />
          <IntervalSection cronjob={cronjob} />
        </>
      );
    }}
  </LayoutCardWithBoundaries>
);
```

### Tab

Manche Seiten verteilen Inhalte auf mehrere Tabs. In solchen Fällen kann es
sinnvoll sein, Tabs separat abzusichern. Dafür eignet sich
`SectionsFragmentWithBoundaries`.

```tsx
export const CronjobTabs: FC = () => (
  <LayoutCard>
    <Tabs>
      <Tab>
        <TabTitle>Allgemein</TabTitle>
        <SectionsFragmentWithBoundaries
          suspenseFallbackProps={{ sectionsCount: 3 }}
        >
          {() => {
            const { cronjobId } = usePathParams("cronjobId");
            const cronjob = CronjobGhost.ofId(cronjobId).getCommon().use();

            return (
              <>
                <ProjectDeactivatedAlert project={cronjob.project} />
                <GeneralSection cronjob={cronjob} />
                <IntervalSection cronjob={cronjob} />
              </>
            );
          }}
        </SectionsFragmentWithBoundaries>
      </Tab>
    </Tabs>
  </LayoutCard>
);
```

## Inhalte einzelner Sections

Enthält eine Section ergänzende **Sekundär**-Daten aus einem anderen Service,
die das primäre Nutzererlebnis nicht einschränken, lohnt sich eine enge
Error-Boundary über `SectionWithErrorBoundary`: Ein Fehler bleibt lokal, während
der Loading-Zustand zur weiten Boundary an der Seiten-Wurzel blubbert.

```tsx
export const AppInstallationSection: FC<Props> = (props) => (
  <SectionWithErrorBoundary>
    {() => {
      const { cronjobGhost } = asGhostProps(props);
      const appInstallation = cronjobGhost
        .getCommon()
        .linkedAppInstallation.getCommon()
        .use();
      const t = useTCronjob();

      return <>...</>;
    }}
  </SectionWithErrorBoundary>
);
```

Lädt die Section unabhängig und sichtbar (eigener Skeleton erwünscht), nutzt man
stattdessen die kombinierte `SectionWithBoundaries` – dann bekommt der Bereich
zusätzlich zum engen Error-Scope auch einen eigenen Loading-Zustand.

**Sonderfall:** Manche Sections werden nur unter bestimmten Bedingungen
angezeigt. Hier soll keine eigene Loading View entstehen, damit User kein
Ladeverhalten sehen, das später zu nichts Sichtbarem führt. Dafür eignet sich
`FragmentWithBoundaries` – es schützt die Section ohne sichtbaren Ladezustand;
das Layout-Primitiv (`<Section>`) bleibt bewusst erhalten.

```tsx
export const AppInstallationSection: FC<Props> = (props) => (
  <FragmentWithBoundaries>
    {() => {
      const { cronjobGhost } = asGhostProps(props);
      const cronjob = cronjobGhost.getCommon().use();
      const t = useTCronjob();

      if (!cronjob.linkedAppInstallation) {
        return null;
      }

      return <Section>...</Section>;
    }}
  </FragmentWithBoundaries>
);
```

## Modals

Modals und Kontextmenüs öffnen erst auf **Interaktion** – eine seiteninterne
Loading-Boundary ergäbe hier keinen Sinn, den Ladezustand trägt die
Flow-Component selbst. Deshalb sind `ModalWithErrorBoundary` und
`ContextMenuWithErrorBoundary` **reine Error-Boundaries**
(`disableSuspenseBoundary`); ein kombiniertes `…WithBoundaries`-Pendant gibt es
bewusst **nicht**.

Für Modals immer `ModalWithErrorBoundary` verwenden. Das verhindert auch, dass
Daten im Modal schon vor dem Öffnen nachgeladen werden.

### Inhalte von Forms in Modals

Eine teilweise geladene oder fehlerhafte Form untergräbt das Vertrauen der User
und kann zu unvollständigen oder inkonsistenten Daten führen. Sobald innerhalb
der Form ein Fehler auftritt, fällt automatisch das ganze Modal in eine
ErrorView, auch wenn es darin noch Boundaries gibt.

```tsx
interface Props {
  cronjob: CronjobGhost;
  controller?: OverlayController;
}

export const RenameCronjobModal: FC<Props> = (props) => (
  <ModalWithErrorBoundary controller={props.controller}>
    {() => {
      const { cronjobGhost } = asGhostProps(props, ["cronjob"]);
      const controller = useModalController();
      const form = useForm();
      const handleOnSubmit = async () => {
        ...
        controller.close();
      };

      return (
        <Form form={form} onSubmit={handleOnSubmit}>
          ...
        </Form>
      );
    }}
  </ModalWithErrorBoundary>
);
```

## Inhalte eines Diagramms

Diagramme beziehen in der Regel viele Daten aus externen Services und sind daher
fehleranfällig. Sie sollten immer von einer Boundary umschlossen werden. Für das
`CartesianChart` gibt es hierfür das `ChartFragmentWithBoundaries`.

```tsx
<ChartFragmentWithBoundaries>
  <CartesianChart>...</CartesianChart>
</ChartFragmentWithBoundaries>
```

## Inhalte einzelner Elemente

Kleine Elemente wie Texte, Links, Badges, Actions oder ContextMenus werden oft
dynamisch geladen, da sie das Nutzungserlebnis der Seite lediglich ergänzen.
Diese sollten daher separat geschützt werden, um nicht die gesamte Seite zu
blockieren. Diese Elemente besitzen oft keine eigene ErrorView und erscheinen im
Fehlerfall nicht oder verbleiben in ihrem Ladezustand.

### Texte

Für Texte gibt es die `TextWithBoundaries` Component, bzw. die
`StringWithBoundaries` Component, je nachdem ob am Ende eine `Text` Component
oder nur ein String gerendert werden soll.

```tsx
<LabeledValue>
  <Label>{t("projectShortId")}</Label>
  <TextWithBoundaries>
    {() => projectGhost.getCommon().use().shortId}
  </TextWithBoundaries>
</LabeledValue>
```

Zusätzlich gibt es Components, die direkt auf ein Model angewendet werden können
und eine eingebaute Boundary besitzen.

```tsx
<ModelLink model={cronjob}>
  <ModelTitle model={cronjob} />
</ModelLink>

<ModelLabeledValue model={cronjob} />
```

### Alerts / Badges

Alerts und Badges ergänzen eine Seite oft nur um kleine Hinweise oder
Statusinformationen. Sie werden in ein `FragmentWithBoundaries` verpackt. So
bleiben sie geschützt, ohne eine eigene Loading View auszulösen – der bare
Fragment-Wrapper ist genau der Fall „bedingter / unsichtbarer Content" mit
null-Fallback.

```tsx
export const CustomerBankruptAlert: FC<Props> = (props) => (
  <FragmentWithBoundaries>
    {() => {
      const { customerGhost, asBadge } = asGhostProps(props, ["customer"]);
      const isBankrupt = customerGhost.getCommon().isBankrupt().use();

      if (!isBankrupt) {
        return null;
      }

      if (asBadge) {
        return <AlertBadge>...</AlertBadge>;
      }

      return <Alert>...</Alert>;
    }}
  </FragmentWithBoundaries>
);
```

### Field

Für Fields kann die `FieldFragmentWithBoundaries` Component verwendet werden.
Sie zeigt im Loading State ein Textfeld, dem optional ein Label mitgegeben
werden kann.

```tsx
export const AppInstallationSelectField: FC<Props> = (props) => {
  const { name, label, projectGhost } = asGhostProps(props, ["project"]);

  return (
    <FieldFragmentWithBoundaries suspenseFallbackProps={{ label }}>
      {() => {
        const appInstallations = projectGhost.appInstallations
          .execute()
          .use().items;

        return (
          <Field name={name}>
            <Select isDisabled={appInstallations.length === 0}>
              <Label>{label}</Label>
              {sortedAppInstallations.map((i) => (
                <Option key={i.id} value={i.id}>
                  {i.description}
                </Option>
              ))}
            </Select>
          </Field>
        );
      }}
    </FieldFragmentWithBoundaries>
  );
};
```

### ContextMenu

Für ContextMenus wird `ContextMenuWithErrorBoundary` verwendet. Um zu
verhindern, dass der Inhalt des ContextMenus springt, werden Elemente die
nachgeladene Inhalte benötigen in ein `MenuItemFragmentWithBoundaries` gepackt;
diese Boundary enthält kein Suspense, damit das ganze ContextMenu im Ladezustand
verbleibt.

```tsx
export const ActionsContextMenu: FC<Props> = (props) => {
  const { appInstallationGhost } = asGhostProps(props);
  const deleteModalController = useModalController();
  const updateModalController = useModalController();

  return (
    <>
      <ContextMenuWithErrorBoundary placement="bottom end">
        {() => {
          const { isBusy } = appInstallationGhost.getCommon().use();
          return (
            <>
              <DeleteMenuItem
                onAction={() => deleteModalController.open()}
                isDisabled={isBusy}
              />
              <MenuItemFragmentWithBoundaries>
                {() => {
                  const updateAvailable = appInstallationGhost
                    .getCommon()
                    .appVersion.updateAvailable()
                    .use();
                  if (!updateAvailable) {
                    return null;
                  }
                  return (
                    <UpdateMenuItem
                      onAction={() => updateModalController.open()}
                    />
                  );
                }}
              </MenuItemFragmentWithBoundaries>
            </>
          );
        }}
      </ContextMenuWithErrorBoundary>

      <DeleteCronjobModal
        controller={deleteModalController}
        appInstallation={appInstallationGhost}
      />
      <UpdateCronjobModal
        controller={updateModalController}
        appInstallation={appInstallationGhost}
      />
    </>
  );
};
```

# Einmalig genutzte Components: keine eigene Boundary für Primärdaten

Detailseiten teilen ihren Inhalt oft in **einmalig genutzte**
`-components/`-Sektionen auf – rein zur Lesbarkeit. Liest eine solche Sektion
**ausschließlich die Primär-Ressource der Seite** (die bereits an der
Seiten-Wurzel aufgelöste Routen-Parameter-Ressource), bekommt sie **keine eigene
Boundary** – weder kombiniert noch error-only. Sie verlässt sich auf die weite
Boundary an der Seiten-Wurzel; eine eigene wäre reine Redundanz. Das gilt, egal
ob die Sektion selbst `entity.getCommon().use()` aufruft (das Suspense blubbert
nach oben) oder die Ressource als Prop bekommt und **kein** Suspense auslöst
(dann greift „kein Suspense → keine Boundary" – das **plain** Layout-Primitiv
genügt).

**Boundary behalten**, sobald **eine** Bedingung zutrifft:

- Die Sektion liest **Sekundärdaten** – eine andere Entity, Relation oder
  Liste/Query, die **nicht** schon an der Seiten-Wurzel aufgelöst ist → enge
  `…WithErrorBoundary` (kombiniert bei sichtbarem, unabhängig ladendem Inhalt).
- Die Component wird **mehrfach oder geteilt** genutzt (alles unter
  `src/shared/`, Listenelemente, aus mehreren Seiten importiert) → über einer
  Verwendung ist keine Seiten-Wurzel-Boundary garantiert.
- **Modals / Kontextmenüs**, die erst beim Öffnen Sekundärdaten laden (siehe
  [Modals](#modals)).

**Fehlt der Seite die Seiten-Wurzel-Boundary komplett**, erst diese ergänzen
(den Seiteninhalt z. B. in `LayoutCardWithBoundaries` /
`SectionsFragmentWithBoundaries` wrappen), **dann** die engen
Sektions-Boundaries entfernen. Achtung: Wird die Primär-Ressource **oberhalb**
des zurückgegebenen Wrappers aufgelöst (`Ghost.ofId(id).…use()` vor dem
`return`), sitzt das Suspense außerhalb der Boundary – die Auflösung muss dann
in den Render-Prop der Wurzel-Boundary wandern.

# Verfügbare Wrapper

**Kombiniert (Loading + Error):** `FragmentWithBoundaries`,
`ContentFragmentWithBoundaries`, `SectionWithBoundaries`,
`SectionsFragmentWithBoundaries`, `LayoutCardWithBoundaries`,
`LayoutCardFragmentWithBoundaries`, `RootLayoutWithBoundaries`,
`SecondarySideNavigationWithBoundaries`, `TextWithBoundaries`,
`StringWithBoundaries`, `LinkWithBoundaries`, `LinkFragmentWithBoundaries`,
`FieldFragmentWithBoundaries`, `ChartFragmentWithBoundaries`,
`AvatarWithBoundaries`, `LoadingSpinnerFragmentWithBoundaries`,
`MenuItemFragmentWithBoundaries`.

**Error-only (`…WithErrorBoundary`):** `FragmentWithErrorBoundary`,
`ContentFragmentWithErrorBoundary`, `SectionWithErrorBoundary`,
`SectionsFragmentWithErrorBoundary`, `LayoutCardWithErrorBoundary`,
`LayoutCardFragmentWithErrorBoundary`, `TextWithErrorBoundary`,
`StringWithErrorBoundary`, `ModalWithErrorBoundary`,
`ContextMenuWithErrorBoundary`.

# Enforcement

- **Syntaktisch, per Lint** (`local/no-raw-boundaries`): kein rohes `<Suspense>`
  und kein direkter Import aus `react-error-boundary` außerhalb von
  `src/shared/boundaries/`. Alles läuft über die Wrapper.
- **Semantisch, per Review anhand dieses Regelwerks**: Kritikalität (primär /
  sekundär), Skeleton-Treue, weit/eng-Platzierung. Diese Urteile sind statisch
  nicht zuverlässig prüfbar und gehören daher in Review + dieses Dokument.

---

# Weitere Empfehlungen

Ein durchdachtes Loading- und ErrorBoundary-Konzept endet nicht bei der
technischen Umsetzung. Fehler sollten zuverlässig erfasst und an
Monitoring-Systeme, Sentry oder Datadog weitergegeben werden, damit Probleme
früh sichtbar werden. Manuelle und automatisierte Tests helfen dabei, die
eigenen Fallbacks regelmäßig zu überprüfen und sicherzustellen, dass sie in
allen relevanten Situationen greifen. Ebenso wichtig ist eine klare und
konsistente Formulierung der Fehlermeldungen, damit User verstehen, was passiert
und wie sie weitermachen können. Für konkrete Hinweise zur sprachlichen
Ausgestaltung bietet die Guideline
[Fehlermeldungen](/02-foundations/03-content-guidelines/03-fehlermeldungen)
weitere Orientierung. So entsteht ein stabiles und nachvollziehbares Verhalten,
das sowohl die Entwicklung als auch die Nutzung der Anwendung unterstützt.
