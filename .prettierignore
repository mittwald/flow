apps/docs/out
node_modules
dist

# Cross-version HTML-output references are exact snapshots compared byte-for-byte
# by toMatchFileSnapshot; reformatting them would break the comparison.
packages/remote-react-components/e2e/cross-version/__html__
.nx
.yarn
.next
.pnp*
pnpm-*
CHANGELOG.md

/.nx/cache
apps/docs/src/lib/liveCode/dynamicImports.ts

/.nx/workspace-data

# GitHub YAML is hand-wrapped for readability (folded `if:` conditions, one
# condition per line). Prettier's reflow is semantically a no-op here but
# destroys that structure, so these stay out of the formatter's scope.
.github/workflows
.github/ISSUE_TEMPLATE

# Generated by fumadocs-mdx on postinstall and gitignored — checking it only
# produces noise in `format:check`.
apps/docs/.source
